import classNames from 'classnames/bind';
import styles from './BookCategory.module.scss';
const cx = classNames.bind(styles);

const BookCategory = () => {
    return (
        <div>
            Trang quản lý danh sách danh mục của amdin
        </div>
    )
}

export default BookCategory;